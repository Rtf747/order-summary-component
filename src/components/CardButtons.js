const CardButtons = () => {
 return (
  <section className='buttons'>
   <button className='Accept'>Proceed to Payment</button>
   <button className='Cancel'>Cancel Order</button>
  </section>
 );
};

export default CardButtons;
